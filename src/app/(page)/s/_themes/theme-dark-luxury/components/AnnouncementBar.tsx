// THEME: dark-luxury — AnnouncementBar

import BaseAnnouncementBar from '../../../_components/AnnouncementBar';
import type { AnnouncementBarProps } from '../../../_lib/types';

export default function DarkLuxuryAnnouncementBar(props: AnnouncementBarProps) {
  return <BaseAnnouncementBar {...props} />;
}
